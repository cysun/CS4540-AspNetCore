@{
    ViewData["Title"] = "Home Page";
}
<h3>Principal Claims</h3>

@if (User.Identity.IsAuthenticated)
{
    @foreach (var claim in User.Claims)
    {
        <p>@claim.Type : @claim.Value </p>
    }
}